[package]
name = "webhook_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8"

# HTTP Client
ureq = { version = "3.3", features = ["json"] }

# JSON Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"